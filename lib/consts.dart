import 'package:flutter/material.dart';

// Custom neon theme colors utilizing Flutter's modern .withValues syntax for transparency
final backgroundColor = Colors.deepPurple.shade900.withValues(alpha: 0.2);
final buttonOne = Colors.deepPurple.shade900.withValues(alpha: 0.7);
final buttonTwo = Colors.purple.shade400.withValues(alpha: 0.8);
final text = Colors.white30.withValues(alpha: 0.6);
final Color inactiveColor = Colors.deepPurple.shade900.withValues(alpha: 0.15);
final Color buttonColor = Colors.purple.shade400.withValues(alpha: 0.3);
